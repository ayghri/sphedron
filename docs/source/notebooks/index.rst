Notebook Examples
=================

These pages are rendered from Jupyter notebooks.

.. note::

   By default, the docs build does not execute notebooks (it renders the saved inputs/outputs).
   To execute notebooks locally during a Sphinx build, set:

   - ``SPHEDRON_DOCS_EXECUTE_NOTEBOOKS=auto``

.. toctree::
   :maxdepth: 1

   regridding_benchmark
   icosphere_transfer
   mesh_transfer
   refine_edge
   Discrete_Laplacian
